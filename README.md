<div align="center">
</div>


## Run Locally

**Requirements:** Node.js


1. Install dependencies:
   `npm install`
2. Set the `GEMINI_API_KEY` in [.env.local](.env.local) to your Gemini API key（非必要选项，可以跳过这一步）
3. Run the app:
   `npm run dev`
